import { PriorityIconPipe } from './priority-icon-pipe';

describe('PriorityIconPipe', () => {
  it('create an instance', () => {
    const pipe = new PriorityIconPipe();
    expect(pipe).toBeTruthy();
  });
});
